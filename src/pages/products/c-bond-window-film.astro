---
/* Hallmark · redesign · macrostructure: numbered-alternating-rows · tone: professional-editorial
 * template: benefits.astro · anchor hue: green (#81AB4C) */
import BaseLayout from '../../layouts/BaseLayout.astro';
import InnerPageHero from '../../components/InnerPageHero.astro';
import BrandLogos from '../../components/BrandLogos.astro';
import { Icon } from 'astro-icon/components';

const title = 'C-Bond Philadelphia | Ballistic Window Film Philadelphia';
const description = 'Protect and enhance glass with C-Bond in Philadelphia, boosting durability, shatter resistance, and long-term safety for homes and businesses.';

const featuredSections = [
  {
    number: '01',
    heading: 'About C-Bond Systems',
    body: "C-Bond's nanotechnology primer eliminates microscopic stress fractures in glass, improving flexibility and shatter resistance. When combined with window film, it dramatically improves adhesion and reduces cure time — making every film installation perform better and last longer. C-Bond is applied before the window film as a primer layer, chemically bonding to the glass substrate to create a stronger, more receptive surface for any film product.",
    img: '/images/c-bond-systems-philadelphia.jpg',
    alt: 'C-Bond nanotechnology systems Philadelphia',
    href: '/benefits/safety-and-security/',
    cta: 'Explore Safety & Security',
  },
  {
    number: '02',
    heading: 'Benefits of C-Bond for Philadelphia Properties',
    body: 'C-Bond optimizes all window film installations, accelerates cure times, enhances film adhesion, and upgrades glass to shatter-resistant quality. It provides superior protection against natural disasters and severe weather, and delivers durable nanotechnology for a long-term glass investment. Philadelphia properties in high-traffic or high-risk environments benefit most — C-Bond ensures every installation delivers maximum performance from day one.',
    img: '/images/c-bond-primer-window-film-philadelphia.jpg',
    alt: 'C-Bond primer benefits Philadelphia',
    href: '/contact/',
    cta: 'Get a Free Quote',
  },
  {
    number: '03',
    heading: 'C-Bond + Window Film: The Complete Solution',
    body: 'C-Bond is most powerful when paired with a high-quality window film. The nanotechnology primer strengthens the glass substrate, while the film adds solar control, UV blocking, privacy, or security performance. Together, they deliver a level of glass protection that neither product achieves alone — at a fraction of the cost of full window replacement. For security-critical applications, C-Bond is the recommended foundation for any film installation.',
    img: '/images/window-film-contractor-philadelphia-2.jpg',
    alt: 'C-Bond window film contractor Philadelphia',
    href: '/contact/',
    cta: 'Schedule a Consultation',
  },
];

const benefitItems = [
  { icon: 'lucide:zap', title: 'Nanotechnology', sub: 'Strengthens glass at the molecular level' },
  { icon: 'lucide:shield-check', title: 'Shatter Resistant', sub: 'Upgraded glass durability' },
  { icon: 'lucide:layers', title: 'Film Adhesion', sub: 'Dramatically improves all film installs' },
  { icon: 'lucide:clock', title: 'Faster Cure Time', sub: 'Accelerates installation performance' },
  { icon: 'lucide:wind', title: 'Weather Defense', sub: 'Superior protection vs. natural disasters' },
  { icon: 'lucide:trending-up', title: 'Film Performance', sub: 'Optimizes every window film product' },
  { icon: 'lucide:lock', title: 'Break-In Defense', sub: 'Ballistic resistance upgrade' },
  { icon: 'lucide:award', title: 'Long-Term Value', sub: 'Durable nanotechnology investment' },
];
---

<BaseLayout title={title} description={description} canonical="https://www.windowfilmphiladelphia.net/products/c-bond-window-film/">

<InnerPageHero
  title="C-Bond Window Film Philadelphia"
  bgImage="/images/c-bond-window-film-philadelphia-2.png"
  desc="Nanotechnology that strengthens glass at the molecular level."
/>

<div class="ip-page-body">

<section class="pp-intro">
  <div class="pp-intro__inner">
    <div class="pp-intro__left">
      <span class="pp-eyebrow">Glass Strengthening</span>
      <h1 class="pp-intro__title">Stronger Glass. Better Film. Zero Replacement.</h1>
    </div>
    <div class="pp-intro__right">
      <p class="pp-intro__lead">C-Bond uses proprietary nanotechnology primer agents to strengthen existing glass at the molecular level — eliminating microscopic imperfections, improving shatter resistance, and dramatically enhancing the adhesion and performance of any window film installed over it. Window Film Philadelphia is an expert C-Bond specialist serving residential and commercial properties throughout Philadelphia.</p>
      <div class="pp-intro__ctas">
        <a href="/contact/" class="pp-btn pp-btn--primary">Get a Free Quote</a>
        <a href="/products/" class="pp-btn pp-btn--ghost">All Products</a>
      </div>
    </div>
  </div>
</section>

<section class="pp-featured">
  <div class="pp-featured__hd">
    <span class="pp-eyebrow">How It Works</span>
    <h2 class="pp-featured__title">The science behind C-Bond nanotechnology</h2>
  </div>

  {featuredSections.map((s, i) => (
    <div class={`pp-feat-row fade-in${i % 2 === 1 ? ' pp-feat-row--rev' : ''}`}>
      <div class="pp-feat-row__img">
        <img src={s.img} alt={s.alt} width="600" height="480" loading="lazy" />
        <span class="pp-feat-row__pill">{s.heading}</span>
      </div>
      <div class="pp-feat-row__content">
        <span class="pp-feat-row__num">{s.number}</span>
        <h3 class="pp-feat-row__heading">{s.heading}</h3>
        <p class="pp-feat-row__body">{s.body}</p>
        <a href={s.href} class="pp-feat-row__cta">
          {s.cta}
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor" viewBox="0 0 16 16" aria-hidden="true"><path fill-rule="evenodd" d="M4.646 1.646a.5.5 0 0 1 .708 0l6 6a.5.5 0 0 1 0 .708l-6 6a.5.5 0 0 1-.708-.708L10.293 8 4.646 2.354a.5.5 0 0 1 0-.708z"/></svg>
        </a>
      </div>
    </div>
  ))}
</section>

<section class="pp-why">
  <div class="pp-why__hd">
    <span class="pp-eyebrow">Key Benefits</span>
    <h2 class="pp-why__title">Eight ways C-Bond strengthens your glass</h2>
  </div>
  <div class="pp-why__grid">
    {benefitItems.map((item) => (
      <div class="pp-why__item fade-in">
        <div class="pp-why__icon"><Icon name={item.icon} width="32" height="32" /></div>
        <div class="pp-why__divider"></div>
        <h4 class="pp-why__item-title">{item.title}</h4>
        <p class="pp-why__item-sub">{item.sub}</p>
      </div>
    ))}
  </div>
</section>

<section class="pp-faq">
  <div class="pp-faq__hd">
    <span class="pp-eyebrow">FAQ</span>
    <h2 class="pp-faq__title">Common questions about C-Bond window film</h2>
  </div>
  <div class="pp-faq__item pp-faq__item--open">
    <div class="pp-faq__q">What is C-Bond and how does it work? <span class="pp-faq__toggle">+</span></div>
    <div class="pp-faq__a">C-Bond is a nanotechnology primer applied to glass before window film installation. It works by penetrating the glass surface and eliminating microscopic stress fractures and imperfections — improving the flexibility and shatter resistance of the glass at the molecular level. It also dramatically improves window film adhesion and reduces the film's cure time, resulting in a better-performing, longer-lasting installation.</div>
  </div>
  <div class="pp-faq__item">
    <div class="pp-faq__q">Do I need C-Bond with every window film installation? <span class="pp-faq__toggle">+</span></div>
    <div class="pp-faq__a">C-Bond is not required for every installation but is highly recommended for security and safety film installs, or in any high-impact or high-risk environment. The Window Film Philadelphia team will advise on whether C-Bond is the right addition for your specific project during the free consultation.</div>
  </div>
  <div class="pp-faq__item">
    <div class="pp-faq__q">Does C-Bond change the appearance of my windows? <span class="pp-faq__toggle">+</span></div>
    <div class="pp-faq__a">No. C-Bond is optically transparent — it creates no visible change in the appearance of your glass. Once applied and the film is installed over it, your windows look exactly as they would with film alone.</div>
  </div>
  <div class="pp-faq__item">
    <div class="pp-faq__q">Can C-Bond be applied to any type of glass? <span class="pp-faq__toggle">+</span></div>
    <div class="pp-faq__a">C-Bond is compatible with most commercial and residential glass types, including single-pane, double-pane, tempered, and laminated glass. The Window Film Philadelphia team always assesses glass type and condition before recommending C-Bond for a specific application.</div>
  </div>
  <div class="pp-faq__item">
    <div class="pp-faq__q">How does C-Bond improve the performance of security window film? <span class="pp-faq__toggle">+</span></div>
    <div class="pp-faq__a">C-Bond strengthens the glass substrate itself while also improving the bond between the glass and the security film. The combined result is significantly higher impact resistance than the film alone would achieve — the glass flexes more before breaking, and the film holds the fragments together longer, providing additional time to respond to a forced entry or impact event.</div>
  </div>
  <div class="pp-faq__item">
    <div class="pp-faq__q">Is C-Bond a replacement for window film? <span class="pp-faq__toggle">+</span></div>
    <div class="pp-faq__a">No. C-Bond is a primer and enhancer used alongside window film — not instead of it. The two products work together: C-Bond strengthens and primes the glass, and the window film provides the performance layer (solar, UV, privacy, or security). Together, they deliver results neither achieves alone.</div>
  </div>
</section>

<BrandLogos />

<div class="pp-cta fade-in">
  <h2>Ready to upgrade your property with <em>C-Bond glass strengthening?</em></h2>
  <div class="pp-cta__btns">
    <a href="/contact/" class="pp-btn pp-btn--white">Get a Free Quote</a>
    <a href="tel:+12673947980" class="pp-btn pp-btn--green">Call (267) 394-7980</a>
  </div>
</div>

</div>
</BaseLayout>

<style>
/* ── PAGE BODY WRAPPER ── */
.ip-page-body {
  position: relative;
  background: #ffffff;
  border-top-left-radius: 40px;
  border-top-right-radius: 40px;
  border-bottom-left-radius: 40px;
  border-bottom-right-radius: 40px;
  margin-top: -40px;
  z-index: 10;
  overflow: visible;
  padding-bottom: 80px;
}
.pp-eyebrow {
  display: inline-block;
  font-family: var(--font-body, 'DM Sans', sans-serif);
  font-size: 11px; font-weight: 700;
  letter-spacing: 2px; text-transform: uppercase;
  color: var(--green, #81AB4C); margin-bottom: 16px;
}
.pp-btn {
  display: inline-block; padding: 14px 28px;
  border-radius: var(--radius-pill, 100px);
  font-family: var(--font-body, 'DM Sans', sans-serif);
  font-size: 15px; font-weight: 500;
  text-decoration: none; transition: all 0.2s;
  border: 2px solid transparent; white-space: nowrap;
}
.pp-btn--primary { background: var(--green, #81AB4C); color: #fff; border-color: var(--green, #81AB4C); }
.pp-btn--primary:hover { background: #6e9640; border-color: #6e9640; transform: translateY(-2px); color: #fff; }
.pp-btn--ghost { background: transparent; color: var(--brand, #272E32); border-color: var(--brand, #272E32); }
.pp-btn--ghost:hover { background: var(--brand, #272E32); color: #fff; transform: translateY(-2px); }
.pp-btn--white { background: #fff; color: var(--brand, #272E32); border-color: #fff; font-weight: 600; }
.pp-btn--white:hover { background: var(--off-white, #F7F6F3); transform: translateY(-2px); }
.pp-btn--green { background: var(--green, #81AB4C); color: #fff; border-color: var(--green, #81AB4C); }
.pp-btn--green:hover { background: #6e9640; transform: translateY(-2px); color: #fff; }
.pp-intro { padding: 80px 10%; border-top-left-radius: 40px; border-top-right-radius: 40px; overflow: hidden; }
.pp-intro__inner { display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; }
.pp-intro__title {
  font-family: var(--font-serif, 'Playfair Display', serif);
  font-size: clamp(36px, 4.5vw, 58px); font-weight: 700;
  line-height: 1.1; letter-spacing: -1px;
  color: var(--text-dark, #1a1a1a); margin: 0;
}
.pp-intro__lead {
  font-family: var(--font-body, 'DM Sans', sans-serif);
  font-size: 17px; color: var(--text-mid, #4a4a4a);
  line-height: 1.75; margin-bottom: 32px;
}
.pp-intro__ctas { display: flex; gap: 14px; flex-wrap: wrap; }
.pp-featured { padding: 100px 10%; background: var(--off-white, #F7F6F3); }
.pp-featured__hd { margin-bottom: 72px; }
.pp-featured__title {
  font-family: var(--font-serif, 'Playfair Display', serif);
  font-size: clamp(32px, 4vw, 52px); font-weight: 700;
  line-height: 1.15; letter-spacing: -0.5px;
  color: var(--text-dark, #1a1a1a); margin: 0;
}
.pp-feat-row { display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; margin-bottom: 100px; }
.pp-feat-row:last-child { margin-bottom: 0; }
.pp-feat-row--rev .pp-feat-row__img { order: 2; }
.pp-feat-row--rev .pp-feat-row__content { order: 1; }
.pp-feat-row__img { position: relative; height: 480px; border-radius: 32px; overflow: hidden; }
.pp-feat-row__img img { width: 100%; height: 100%; object-fit: cover; display: block; }
.pp-feat-row__pill {
  position: absolute; bottom: 24px; left: 24px;
  background: rgba(255,255,255,0.92); backdrop-filter: blur(8px);
  padding: 10px 18px; border-radius: 100px;
  font-family: var(--font-body, 'DM Sans', sans-serif);
  font-size: 13px; font-weight: 600; color: var(--brand, #272E32);
  box-shadow: 0 4px 16px rgba(0,0,0,0.1);
}
.pp-feat-row__num {
  display: block; font-family: var(--font-serif, 'Playfair Display', serif);
  font-size: 72px; font-weight: 700; color: var(--green, #81AB4C);
  line-height: 1; opacity: 0.25; margin-bottom: -8px;
}
.pp-feat-row__heading {
  font-family: var(--font-serif, 'Playfair Display', serif);
  font-size: clamp(24px, 3vw, 36px); font-weight: 700;
  color: var(--text-dark, #1a1a1a); line-height: 1.2;
  margin-bottom: 20px; letter-spacing: -0.3px;
}
.pp-feat-row__body {
  font-family: var(--font-body, 'DM Sans', sans-serif);
  font-size: 16px; color: var(--text-mid, #4a4a4a);
  line-height: 1.8; margin-bottom: 28px;
}
.pp-feat-row__cta {
  display: inline-flex; align-items: center; gap: 8px;
  font-family: var(--font-body, 'DM Sans', sans-serif);
  font-size: 15px; font-weight: 600; color: var(--green, #81AB4C);
  text-decoration: none; transition: gap 0.2s;
}
.pp-feat-row__cta:hover { gap: 14px; }
.pp-why { background: var(--brand, #272E32); padding: 100px 10%; }
.pp-why__hd { margin-bottom: 64px; }
.pp-why__hd .pp-eyebrow { color: var(--green, #81AB4C); }
.pp-why__title {
  font-family: var(--font-serif, 'Playfair Display', serif);
  font-size: clamp(28px, 3.5vw, 48px); font-weight: 700;
  color: #ffffff; line-height: 1.15; letter-spacing: -0.5px;
  margin: 0; max-width: 600px;
}
.pp-why__grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0; }
.pp-why__item { padding: 32px 28px; border-right: 1px solid rgba(255,255,255,0.1); border-bottom: 1px solid rgba(255,255,255,0.1); }
.pp-why__item:nth-child(4n) { border-right: none; }
.pp-why__item:nth-child(n+5) { border-bottom: none; }
.pp-why__icon { color: var(--green, #81AB4C); margin-bottom: 16px; display: flex; align-items: center; line-height: 1; }
.pp-why__icon svg { display: block; }
.pp-why__divider { width: 32px; height: 2px; background: var(--green, #81AB4C); margin-bottom: 16px; }
.pp-why__item-title { font-family: var(--font-body, 'DM Sans', sans-serif); font-size: 15px; font-weight: 700; color: #ffffff; margin-bottom: 6px; }
.pp-why__item-sub { font-family: var(--font-body, 'DM Sans', sans-serif); font-size: 13px; color: rgba(255,255,255,0.5); margin: 0; line-height: 1.5; }
.pp-faq { padding: 100px 10%; background: #ffffff; }
.pp-faq__hd { margin-bottom: 48px; }
.pp-faq__title {
  font-family: var(--font-serif, 'Playfair Display', serif);
  font-size: clamp(28px, 3.5vw, 48px); font-weight: 700;
  line-height: 1.15; letter-spacing: -0.5px;
  color: var(--text-dark, #1a1a1a); max-width: 560px; margin: 0;
}
.pp-faq__item { border-bottom: 1px solid var(--light-gray, #EFEFED); padding: 24px 0; cursor: pointer; max-width: 900px; }
.pp-faq__item:first-of-type { border-top: 1px solid var(--light-gray, #EFEFED); }
.pp-faq__q { font-family: var(--font-body, 'DM Sans', sans-serif); font-size: 17px; font-weight: 600; color: var(--text-dark, #1a1a1a); display: flex; justify-content: space-between; align-items: center; gap: 12px; }
.pp-faq__toggle { color: var(--green, #81AB4C); font-size: 22px; flex-shrink: 0; transition: transform 0.2s; }
.pp-faq__a { font-family: var(--font-body, 'DM Sans', sans-serif); font-size: 15px; color: var(--text-mid, #4a4a4a); line-height: 1.75; margin-top: 14px; display: none; }
.pp-faq__item--open .pp-faq__a { display: block; }
.pp-faq__item--open .pp-faq__toggle { transform: rotate(45deg); }
.pp-cta {
  background: var(--brand, #272E32); border-radius: 40px;
  padding: 64px 60px; display: flex; align-items: center;
  justify-content: space-between; gap: 40px; margin: 0 5% 0;
}
.pp-cta h2 { font-family: var(--font-serif, 'Playfair Display', serif); font-size: 32px; font-weight: 700; color: #fff; line-height: 1.2; max-width: 460px; margin: 0; }
.pp-cta h2 em { font-style: italic; color: var(--green, #81AB4C); }
.pp-cta__btns { display: flex; gap: 14px; flex-shrink: 0; }
.fade-in { opacity: 0; transform: translateY(24px); transition: opacity 0.65s ease, transform 0.65s ease; }
.fade-in.visible { opacity: 1; transform: translateY(0); }
@media (max-width: 1024px) {
  .pp-intro__inner { grid-template-columns: 1fr; gap: 40px; }
  .pp-feat-row { grid-template-columns: 1fr; gap: 40px; }
  .pp-feat-row--rev .pp-feat-row__img { order: 0; }
  .pp-feat-row--rev .pp-feat-row__content { order: 1; }
  .pp-why__grid { grid-template-columns: repeat(2, 1fr); }
  .pp-why__item:nth-child(4n) { border-right: 1px solid rgba(255,255,255,0.1); }
  .pp-why__item:nth-child(2n) { border-right: none; }
}
@media (max-width: 768px) {
  .pp-intro { padding: 60px 5%; }
  .pp-featured { padding: 60px 5%; }
  .pp-why { padding: 60px 5%; }
  .pp-faq { padding: 60px 5%; }
  .pp-feat-row__img { height: 300px; }
  .pp-why__grid { grid-template-columns: 1fr; }
  .pp-cta { flex-direction: column; padding: 40px 28px; text-align: center; }
  .pp-cta h2 { font-size: 26px; max-width: 100%; }
  .pp-cta__btns { flex-direction: column; width: 100%; }
}
@media (max-width: 480px) {
  .pp-intro__ctas { flex-direction: column; }
}
</style>

<script>
  const obs = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
  }, { threshold: 0.1 });
  document.querySelectorAll('.fade-in').forEach(el => obs.observe(el));

  document.querySelectorAll('.pp-faq__item').forEach(item => {
    item.querySelector('.pp-faq__q')?.addEventListener('click', () => {
      const isOpen = item.classList.contains('pp-faq__item--open');
      document.querySelectorAll('.pp-faq__item').forEach(i => i.classList.remove('pp-faq__item--open'));
      if (!isOpen) item.classList.add('pp-faq__item--open');
    });
  });
</script>
